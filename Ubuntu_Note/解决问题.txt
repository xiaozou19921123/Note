1）解决ubuntu E: 无法获得锁 /var/lib/apt/lists/lock - open (11: 资源暂时不可用) 
sudo rm -f /var/lib/apt/lists/lock
2)解决上网慢问题
sudo gedit /etc/modprobe.d/iwlwifi.conf
空白出添加下面代码
options iwlwifi 11n_disable=1
3)由于没有公钥，无法验证下列签名
gpg --keyserver pgpkeys.mit.edu --recv-key [修改对应9AA38DCD55BE302B]
gpg -a --export  9AA38DCD55BE302B | sudo apt-key add -
重新更新源：
sudo apt-get update
4)合上盖子不做任何处理&按下电源键处理
sudo gedit /etc/systemd/logind.conf
#HandleLidSwitch=suspend改为HandleLidSwitch=ignore
